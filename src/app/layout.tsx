import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Racheal Acio – Software & AI Engineer",
  description: "Results-driven AI engineer specialising in AI agents and data-driven applications",
  keywords: "AI Engineer, Software Developer, LangChain, Python, Django, Uganda",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
